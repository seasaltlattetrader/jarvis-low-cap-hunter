const BIRD='https://public-api.birdeye.so';const hdr=k=>({'X-API-KEY':k,'x-chain':'solana','accept':'application/json'});const num=v=>Number.isFinite(Number(v))?Number(v):0;async function get(u,k){const r=await fetch(u,{headers:hdr(k),cache:'no-store'});const t=await r.text();let j;try{j=JSON.parse(t)}catch{j={}}if(!r.ok){const e=new Error(`HTTP ${r.status}`);e.status=r.status;throw e}return j}
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='POST')return res.status(405).json({ok:false,error:'POST only'});const key=process.env.BIRDEYE_API_KEY||'';if(!key)return res.status(200).json({ok:false,error:'Birdeye key missing',decision:'INCOMPLETE — Birdeye key not configured'});const address=req.body?.address||'';if(!address)return res.status(400).json({ok:false,error:'Token address required'});const out={ok:true,address,checks:{auth:null,smartMoney:null,holderProfile:null,security:null},decision:'UNKNOWN',conviction:null};
try{await get(`${BIRD}/defi/price?address=So11111111111111111111111111111111111111112`,key);out.checks.auth={status:200,authenticated:true}}catch(e){out.checks.auth={status:e.status||0,authenticated:false,error:e.message};if(e.status===401||e.status===403)return res.status(200).json({...out,decision:`INCOMPLETE — Birdeye authentication failed (${e.status})`,conviction:0});if(e.status===429)return res.status(200).json({...out,decision:'COOLDOWN — Birdeye rate limited during authentication check',conviction:0})}
try{let sm=null,holder=null,sec=null;try{const j=await get(`${BIRD}/smart-money/v1/token/list?interval=1d&trader_style=all&sort_by=net_flow&sort_type=desc&offset=0&limit=100`,key);const rows=j?.data?.items||j?.data||[];sm=rows.find(x=>String(x.address||x.token_address||x.tokenAddress||x.mint||'').toLowerCase()===address.toLowerCase())||null;out.checks.smartMoney={status:200,matched:Boolean(sm),smartTraders:num(sm?.smart_traders_no||sm?.smartTradersNo),netFlow:num(sm?.net_flow||sm?.netFlow)}}catch(e){out.checks.smartMoney={status:e.status||0,error:e.message}}try{const j=await get(`${BIRD}/token/v1/holder-profile?token_address=${encodeURIComponent(address)}&interval=1h&ui_amount_mode=raw&include_zero_balance=false`,key);const d=j?.data||{},tags=d.tags||{},share=k=>num(tags?.[k]?.percent_of_supply||tags?.[k]?.supply_share),count=k=>num(tags?.[k]?.holder_count||tags?.[k]?.count),suspicious=share('bundler')+share('sniper')+share('insider')+share('dev');holder={smartTraders:count('smart_trader'),smartSupply:share('smart_trader'),suspicious,top10:num(d.top10_holder?.percent_of_supply||d.top10_holder?.supply_share)};out.checks.holderProfile={status:200,...holder}}catch(e){out.checks.holderProfile={status:e.status||0,error:e.message}}try{const j=await get(`${BIRD}/defi/token_security?address=${encodeURIComponent(address)}`,key);sec=j?.data||{};out.checks.security={status:200,available:true}}catch(e){out.checks.security={status:e.status||0,error:e.message}}const smartPts=(sm?45:0)+Math.min(30,num(holder?.smartTraders)*6)+Math.min(15,num(holder?.smartSupply)*2)+(num(sm?.net_flow||sm?.netFlow)>0?10:0),safety=90-Math.min(35,num(holder?.suspicious)*1.7)-Math.max(0,num(holder?.top10)-40)*.4-(out.checks.security.status===200&&((sec.is_honeypot===true)||(sec.isHoneypot===true))?45:0);out.conviction=Math.max(0,Math.min(100,Math.round(smartPts*.5+Math.max(0,safety)*.5)));if(out.checks.smartMoney.matched&&out.checks.holderProfile.status===200&&out.checks.security.status===200)out.decision=out.conviction>=80?'INVESTIGATE':'WATCH';else if([out.checks.smartMoney.status,out.checks.holderProfile.status,out.checks.security.status].includes(429))out.decision='COOLDOWN — Birdeye rate limited';else out.decision='INCOMPLETE — key evidence unavailable';return res.status(200).json(out)}catch(e){return res.status(200).json({...out,error:e.message,decision:'ERROR'})}}
